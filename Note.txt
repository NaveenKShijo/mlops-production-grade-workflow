NOTE:
* naveen-mlflow-artifacts2026 is not specified anywhere here, but it is the bucket where artifacts(scaler.pkl) will be stored. You passed it via MLflow server creation in SageMaker.


Inline policy(JSON) to give access to AmazonSageMaker-ExecutionRole-20260726T132293 the permission for mlflow ui:
{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Effect": "Allow",
            "Action": [
                "sagemaker-mlflow:AccessUI",
                "sagemaker-mlflow:*"
            ],
            "Resource": "arn:aws:sagemaker:eu-north-1:565265042094:mlflow-tracking-server/my-mlflow-server"
        }
    ]
}

FUTURE IMPROVEMENTS:

* Move run_training.py from sagemaker v2(deprecated) to v3

* Inference app/test disagree — main.py:30 returns {"Prediction": [...]}, but test_inference.py:40-43 asserts the response is a bare list. Test fails if enabled.

* Private repo clone would fail — entrypoint.sh:10 uses a tokenless public clone URL; GITHUB_TOKEN is commented out in ci.yaml:61 and never forwarded in run_training.py's environment. Works only if repo is public.

* run_training.py:16 SAGEMAKER_OUTPUT_PATH has no default → if CI forgets it, the SDK silently uses a default bucket and deploy.py's path breaks.

* tests + pytest steps are commented out in both workflows  